An error occurred while running `{{.Command}}`.

Error message:
{{.ErrorMessage}}

You MUST complete every step below in exact order. Do NOT skip ahead.

## STEP 1 — DIAGNOSE

Call the `azd_error_troubleshooting` tool now. Pass the full error message above as input.
Wait for the tool result before continuing.

## STEP 2 — EXPLAIN TO THE USER

Using the tool result from Step 1, respond to the user with two sections:

**What happened**
One to two sentences describing what the error means.

**Why it happened**
One to three sentences explaining the root cause.

STOP here. End your response after the explanation.
Do NOT propose a fix. Do NOT make any file changes. Do NOT provide fix steps.
