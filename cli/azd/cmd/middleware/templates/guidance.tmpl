The user saw the following error while running `{{.Command}}`.

Error message:
{{.ErrorMessage}}

Provide ONLY the actionable fix steps as a short numbered list (max 5 steps).
Each step must be one sentence. Include exact commands where possible.

Do NOT repeat the explanation. Do NOT return JSON. Do NOT make any file changes.