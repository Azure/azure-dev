# Minimal dependency set so `azd ai agent init` (existing-code flow) detects a Python
# project. Keep this lightweight — the Tier 1 existing-code scenarios only scaffold
# and do not install or run the agent.
azure-ai-projects
