List the project's evaluators, or the built-in ones.

Usage
  azd ai eval evaluator list [flags]

Flags
      --all                     : Show every row.
      --builtin                 : List the built-in evaluators instead of the project's own.
      --limit int               : Rows to show. Defaults to 20.
      --project-endpoint string : Foundry project endpoint.

Global Flags
  -C, --cwd string         : Sets the current working directory
      --debug              : Enables debug and diagnostics logging
  -e, --environment string : The name of the environment to use
  -h, --help               : help for list
      --no-prompt          : Runs without prompts. Uses existing values; fails if any required value or decision cannot be resolved automatically.
  -o, --output string      : The output format (default "default")

Examples
  List custom evaluators in the selected project
    azd ai eval evaluator list

  List built-in evaluators
    azd ai eval evaluator list --builtin
