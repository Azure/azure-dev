Delete a generation job record.

The artifact the job produced is already registered as its own version and is not affected.

Usage
  azd ai eval job delete <job-id> [flags]

Flags
      --dataset                 : Required (or --evaluator). Act on dataset generation jobs.
      --evaluator               : Required (or --dataset). Act on evaluator generation jobs.
      --force                   : Delete without asking for confirmation.
      --project-endpoint string : Foundry project endpoint.

Global Flags
  -C, --cwd string         : Sets the current working directory
      --debug              : Enables debug and diagnostics logging
  -e, --environment string : The name of the environment to use
  -h, --help               : help for delete
      --no-prompt          : Runs without prompts. Uses existing values; fails if any required value or decision cannot be resolved automatically.
  -o, --output string      : The output format (default "default")

Examples
  Delete a dataset generation job record after confirmation
    azd ai eval job delete job_123 --dataset
