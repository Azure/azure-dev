# `azd` Finetune Extension

An azd Finetune extension

## Command help

The public command namespace is `azd ai finetuning`.
Run `azd ai finetuning --help` for project and environment guidance, or append
`--help` to any command for flags and examples. Help does not require Azure
authentication. `NO_COLOR=1` disables help colors.
