# Release History

## 0.1.0-preview (Unreleased)

### Features Added

- Add live latency assessments for deployed Azure OpenAI model workloads.
- Compare observed TBT P95 with the current-offer target and exact LLM-Runner benchmark cohorts.
- Generate customer-safe terminal, JSON, and self-contained HTML reports that match the standalone CLI renderer.
- Open generated HTML reports in the foreground through the Windows shell using correctly escaped local file URLs.
- Add six deterministic demo scenarios for exploring decision paths without Azure access.
