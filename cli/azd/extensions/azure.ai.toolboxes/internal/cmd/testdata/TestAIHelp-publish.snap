Set the default version for a toolbox.

This promotes a previously created version so that consumers referencing the
toolbox without an explicit version will receive it. To create a new version,
use 'connection add', 'connection remove', 'skill add', or 'skill remove'.

Usage
  azd ai toolbox publish <name> <version> [flags]

Global Flags
  -C, --cwd string              : Sets the current working directory
      --debug                   : Enables debug and diagnostics logging
  -e, --environment string      : The name of the environment to use
  -h, --help                    : help for publish
      --no-prompt               : Runs without prompts. Uses existing values; fails if any required value or decision cannot be resolved automatically.
  -o, --output string           : The output format (supported: table, json) (default "table")
      --project-endpoint string : Foundry project endpoint URL. When unset, falls back to the active azd environment, azd user config, then FOUNDRY_PROJECT_ENDPOINT.

Examples
  Promote an existing version to the toolbox default
    azd ai toolbox publish research 2
