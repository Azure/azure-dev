name: containerapp
metadata: 
  template: azd-test/containerapptest@v1
services:
  web:
    project: src/dotnet
    module: web
    host: containerapp
    language: csharp
    docker:
      platform: linux/amd64
