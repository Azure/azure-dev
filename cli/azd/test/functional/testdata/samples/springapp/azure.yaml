name: webapp
metadata: 
  template: azd-test/webapptest@v1
services:
  sweb:
    project: ./
    host: springapp
    language: java
    spring:
      deploymentName: default
infra:
  provider: terraform

